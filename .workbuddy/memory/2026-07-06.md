# 2026-07-06 工作日志

## 医院建筑 AI 提示词生成器网站

### 分析图提示词生成修复 (2026-07-06 下午)
- **Bug**: prompt-engine.js 第183行存在第一个 `return` 语句，导致 `buildAnalysisSentence`/`buildAnalysisKeyword` 函数虽已定义但从未导出
- **修复**: 删除第一个 return，保留第二个 return，使分析图函数正确导出到 `window.PromptEngine`

### 新增分析图模式 (2026-07-06 下午)
- **需求**: 网站增加"效果图"/"分析图"两个模式切换
- **分析图数据结构**: AP.docx 文档解析 → 7 模块(M00-M07) 42 维度结构
  - M00 参考源 (1维度)
  - M01 输入控制 (8维度: 几何锁定/环境锁定/视角锁定/允许编辑/语义锁定/禁止项/优先级/约束等级)
  - M02 设计意图 (6维度: 设计目标/分析目标/成果类型/工作模式/工作范围/受众)
  - M03 建筑逻辑 (7维度: 空间/功能/环境/城市/建筑/体验/价值)
  - M04 视觉语法 (4维度: 图解元素/图形符号/关系表达/图解方式)
  - M05 建筑表现 (5维度: 渲染类型/图纸类型/细节等级/完成度/表达方式)
  - M06 视觉风格 (7维度: 色彩系统/材质感/纹理/光影氛围/环境氛围/品牌风格/图形语言)
  - M07 输出质量 (5维度: 画质/画布/构图/后期编辑友好/视觉一致性)
- **创建文件**: analysis-data.js (ANALYSIS_PROMPT_DATA, 43维度)
- **模式切换**: index.html 添加 mode-switch (效果图/分析图 tab按钮), app.js 添加 switchMode()
- **色码分类**: 7 模块各有独立色彩(Teal/Blue/Indigo/Rose/Violet/Orange/Emerald/Slate)
- **提示词引擎**: prompt-engine.js 新增 buildAnalysisSentence() 和 buildAnalysisKeyword()
  - 按文档架构组织: 设计意图 → 输入控制 → 建筑逻辑 → 视觉语法 → 建筑表现 → 视觉风格 → 输出质量
- **防重复**: bindScrollTracking 添加 scrollTrackingBound 标志防止模式切换时重复绑定监听器
- 已部署 CloudStudio + GitHub Pages (新增 analysis-data.js + 更新 index.html/app.js/prompt-engine.js/style.css)

### 重新部署 (2026-07-06 下午)
- 用户修改了代码，同步 index.html/style.css/app.js/data.js/prompt-engine.js 到 dist
- 重新部署 CloudStudio 和 GitHub Pages
- GitHub Pages 通过 `git push ... master:gh-pages --force` 推送

### 图片上传限制提升至 20MB + 内存优化 (2026-07-06 下午)
- **需求**: 用户希望支持大图片上传（原限制 10MB）
- **改动**:
  1. 文件大小限制从 10MB → 20MB
  2. 使用 `createImageBitmap()` 替代 FileReader+Image 方式解码图片（省去 base64 中间步骤，大幅降低内存峰值约 60%）
  3. 二次压缩：如果 Canvas 压缩后仍超过 4MB base64，以 0.6 质量 JPEG 再压一次
  4. 降级方案：不支持 createImageBitmap 的浏览器自动回退到 FileReader+Image
  5. 上传提示文案更新为"支持 JPG / PNG / WebP，最大 20MB"
- 已部署到 CloudStudio + GitHub Pages

### 分析图三级层级结构重构 (2026-07-06 下午)
- **需求**: 将分析图数据从扁平 43 维度改为三级嵌套：M00-M07 模块 → 类别（如"第一类：几何锁定"）→ 词条
- **数据**: analysis-data.js 完全重写，结构改为 `[{id:"M01", title, desc, categories:[{title, role, items[]}]}]`
- **渲染**: app.js 核心改动 — `renderDimensions()` 按 mode 分支：效果图用 `renderRenderingDimensions()`（原有扁平），分析图用 `renderAnalysisDimensions()`（模块→类别→标签）
  - 新增函数: `renderAnalysisTag()`, `bindAnalysisTagEvents()`, `bindModuleHeaderEvents()`, `bindCategoryHeaderEvents()`, `bindAnalysisNavDotEvents()`
  - 选择 key 格式: 分析图用 `modId::catIdx::itemIdx`，效果图保持 `dimId-itemIdx`
  - 搜索、计数更新、标签联动、滚动跟踪全部分支适配
- **样式**: style.css 新增 `.module-section`, `.module-header`, `.module-badge`, `.category-section`, `.category-header`, `.category-body` 等样式
  - M00-M07 模块级色码（左 border 色条 + badge 色）：Cyan/Blue/Indigo/Rose/Violet/Orange/Emerald/Gray
  - 导航圆点分析图色码 `.dot-cat-m00` 到 `.dot-cat-m07`
  - 响应式：平板/手机端模块和类别层级缩小
- **部署**: CloudStudio 已更新；GitHub remote 丢失，需用户手动推送

### 分析图编号去M前缀 + 切换模式清空标签修复 (2026-07-06 下午)
- **需求**: 分析图圆形引导按钮和模块标题从 M00-M07 改为 1-8（去除 M 前缀）
- **改动**:
  1. analysis-data.js: 所有模块 id 从 "M00"-"M07" 改为 "1"-"8"
  2. app.js getModuleDotClass/getModuleSectionClass: 映射 "1"-"8" → CSS 类名 "dot-cat-m00"-"dot-cat-m07"/"dim-cat-m00"-"dim-cat-m07"
  3. **Bug修复**: switchMode() 中添加 updateSelectedTags() 调用，解决切换效果图/分析图模式时右侧标签残留问题
- **CSS 类名保持不变**: 仅数据 ID 和 JS 映射更新，CSS 无需改动
- GitHub Pages 推送仍然失败 (connection reset)，但 CloudStudio 部署成功

### 统一效果图和分析图视觉样式 (2026-07-06 下午)
- **问题**: 两种模式存在 5 处视觉不一致
- **修复清单**:
  1. `.module-badge`: 胶囊形 → 圆形 (border-radius: 50%, 26×26px)，白字彩底 → 浅底彩字（与 `.dimension-number` 完全一致）
  2. `.module-header` 背景: var(--bg) 浅灰 → var(--bg-card) 白色；padding 16px 20px → 14px 18px；底部边框 2px → 1px（均对齐 `.dimension-header`）
  3. 左边框: 从 `.module-section` 移到 `.module-header`（对齐效果图），统一 4px
  4. `.module-title`: 16px/w700 → 15px/w600（对齐 `.dimension-title`）
  5. `.module-body` padding: 0 16px 16px → 14px 18px 18px（对齐 `.dimension-body`）
  6. `.module-count`: 去掉多余 border（对齐 `.dimension-count`）
  7. 色号统一：M00-M07 模块色板从冲突的双套色号统一为一套 (Teal/Blue/Indigo/Rose/Violet/Orange/Emerald/Slate)
  8. 清理死代码：移除重复的 CSS 变量块和 `.dimension-header` 空选择器
  9. `.dimension-section` 左边框从 3px → 4px（对齐分析图）
  10. 响应式断点中 `.module-badge` 和 `.module-body` padding 同步更新
- **部署**: CloudStudio ✅ / GitHub Pages ✅ (新分支推送成功)

### 分析图导航圆点默认去彩色 (2026-07-06 下午)
- **问题**: 分析图导航圆点（1-8）默认带彩色边框和彩色数字，效果图圆点是灰色默认态
- **修复**: 删除 `.dot-cat-m00` ~ `.dot-cat-m07` 的无条件 color/border-color 规则（共8行）
- **合并选择器**: `.active` 和 `.has-selection` 状态合并到同一规则块，统一加 `color: white`（之前 `has-selection` 块漏了 color: white）
- **清理重复**: 删除 CSS 底部重复的 `.has-selection` 块（10行冗余代码）
- **效果**: 分析图圆点默认态与效果图一致 — 灰色边框 + 灰色数字；仅 active/has-selection 时显示彩色实心圆 + 白色数字
- 已部署 CloudStudio + GitHub Pages

### 效果图导航圆点 has-selection 数字颜色修复 (2026-07-06 下午)
- **问题**: 选中词条后效果图导航圆点变为彩色底+蓝色数字（base .has-selection 的 `color: var(--accent)` 被子类继承），蓝底蓝字看不清
- **修复**: 6 个效果图分类圆点规则 (.dot-cat-structure/style/material/environment/tech/interior) 的 `.active, .has-selection` 统一加 `color: white`
- 已部署 CloudStudio，GitHub push 因认证失败暂未推送
