# 2026-07-07 工作日志

## 医院建筑 AI 提示词生成器网站

### 中文提示词生成修复 (2026-07-07 上午)
- **Bug 1**: 效果图 + 中文生成 → 中英混杂（如 "The 手术室 of a 装饰艺术 消毒供应中心."）
  - **根因**: `buildSentence()` 使用 `pickText()` 取到中文词值，但所有连接词（The, of a, render of, organized as 等）硬编码为英文
  - **修复**: 新增 `buildSentenceCN()` — 完整中文语法结构，使用中文连接词（"的"、"采用"、"饰面"、"色调"、"照明"、"营造"、"注重"等）和中文标点（，。）
  - `buildSentence()` 改为 dispatcher: `lang === 'cn'` → `buildSentenceCN()`，否则 → `buildSentenceEN()`

- **Bug 2**: 分析图 + 中文/双语 → 只有英文
  - **根因 1**: `analysis-data.js` 词条只有 `{label, en}` 无 `cn` 字段。`app.js buildSelectionsList()` 传 `cn: val.item.cn`（undefined）→ `pickText()` 回退到 `en`
  - **修复 1**: `buildSelectionsList()` 分析图分支改为 `cn: val.item.label`（label 就是中文）
  - **根因 2**: `buildAnalysisSentence()` 同样硬编码英文连接词
  - **修复 2**: 新增 `buildAnalysisSentenceCN()` — 完整中文语法，13 个模块全部中文连接词
  - `buildAnalysisSentence()` 改为 dispatcher

- **双语模式修复**: `updatePrompt()` 新增分析图 'both' 模式处理 — 追加 `中文参考：` + 中文标签列表
- **关键词模式修复**: `buildKeywordPhrase()` render_param 在 cn 模式下使用 `it.cn || it.en`
- **测试更新**: CN 测试用例从 `'A 急诊科, in a 暖白 color palette.'` 更新为 `'急诊科，暖白色调。'`，19/19 通过
- **部署**: CloudStudio ✅ / GitHub push 认证失败，commit 已本地保存

## 步骤向导重构 + 关键词树状图 (2026-07-07 下午)
- **Change 1 关键词树状图**: 关键词模式改为层级树输出
  - 新增 `buildKeywordTree()`（app.js）：分析图按 `模块→类别→词条`，效果图按 `维度→词条`
  - 格式：`01-参考源:参考建筑` / `02-输入控制-几何锁定:建筑轮廓` / `-环境锁定:湖岸线`
  - 模块/维度编号用连续计数器（跨已选模块顺序 01,02,03，无跳号）；同类下续行用 `-词条`
  - `shortCatTitle()` 去掉"第X类："前缀和"（...）括号；输入控制词条锁定后追加 `（锁）`
  - `updatePrompt()` keyword 分支统一走 `buildKeywordTree()`，'both' 追加逻辑加 `format!=='keyword'` 守卫
- **Change 2 全屏步骤向导**: 左侧维度树完全替换为向导
  - 步骤粒度=大类(category)：分析图=所有模块的所有 category；效果图=15 个 dimension
  - `buildSteps()` 扁平化；`renderWizard()` 结构=stepbar(顶部步骤条) + content(单步内容) + nav(上一步/下一步)
  - 步骤条：当前步骤高亮(active)、已完成(accent 填充)、未完成置灰(future)；点圆点可跳转；底部进度文案 `X/N · 类别` + 进度条
  - 底部导航：`上一步`(首步禁用) / `下一步`(末步变"生成/复制"并复制提示词)
  - 定制控件（标签为主+少量定制）：
    - 参考源(模块1)：图片上传(压缩1200px) + 建筑名称文本输入
    - 选中"参考配色"时显示色板选取器(palette)，结果追加到提示词
    - 输入控制(control_*)词条带🔒锁定开关，存 `val.locked` 并反映到树状图
  - 右侧 prompt 预览面板保留（实时显示）；清空/图片解析/语言/格式切换逻辑不变
  - 测试：prompt-engine 19/19 通过；app.js/style.css 语法检查通过
- **部署**: CloudStudio ✅ (https://8f8b24d09f264b94b6d00ed50d365b07.app.codebuddy.work)
- **GitHub**: `gh` 未安装、HTTPS 无凭据，push 需用户手动 `git push origin feature/prompt-sentence-engine`（已本地 commit dba1c33）

## 分析图按模块分步骤 (2026-07-07 下午，续)
- **需求**: 分析图向导之前按 category 拆步太密（~38步），改为按 **8 个模块** 分步骤（1-参考源 … 8-输出质量）
- **改动**:
  - `buildSteps()` 分析图分支：每模块=一步，`isModule: true`、`catIdx: -1`、聚合 `items` 为 `{item,catIdx,itemIdx,catTitle}[]`
  - `renderWizardContent()` 分析图：`step.isModule` 时按 category 分组渲染（`.wizard-cat-group` / `.wizard-cat-title` / `.wizard-cat-tags`）
  - `stepHasSelection()` / `countStepSelections()` 模块步只按 `moduleId` 判定（忽略 catIdx），计数与高亮正确
  - 新增 style.css 类别分组样式
- **效果**: 分析图步骤 38→8；每步内默认按类别分组，结构清晰不堆叠
- **部署**: CloudStudio ✅；GitHub push 失败需手动（已本地 commit 6777f03）

## 分析图三项优化 (2026-07-07 下午)
- **1. 移除锁图标**: 分析图输入控制(模块2)词条后的 🔒 锁图案全部去掉
  - 彻底移除 lock 机制：`renderAnalysisTag` 不再渲染 `tag-lock-toggle`；`bindWizardTagEvents`/`bindWizardCustomEvents` 移除锁绑定；删除 `toggleLock()`；`buildKeywordTree` 移除 `（锁）` 后缀；删除 CSS `.tag-lock-toggle`
  - 决策：锁图标 + （锁）中文后缀一起移除（既去图标 clutter，又解决英文模式夹带中文）。若用户想保留"锁定/编辑"语义，可改为步骤头部的分类级开关，届时再议
- **2. 英文关键词夹带中文修复**: 同上去掉 `（锁）` 后缀；`itemText()` 本身 en 模式只返回 `item.en`，无其它中文来源
- **3. 关键词树状图换行**: `.prompt-text` 加 `white-space: pre-wrap`，树状 `\n` 正常换行（01/02/03 各占一行）
- **测试**: prompt-engine 19/19 通过；无 dangling lock 引用
- **部署**: CloudStudio ✅；GitHub push 失败需手动（已本地 commit 00b7a30）

## 关键词英文模式前缀英文化 (2026-07-07)
- **需求**: 英语模式下关键词树状图的前缀（模块名/类别名/维度名）仍为中文，如 `01-输入控制-几何锁定:Building morphology / form`，要求前缀也变英文
- **实现**: 在 app.js `buildKeywordTree()` 前新增三张英文映射表
  - `MODULE_EN`（分析模块 id→英文，8 项）、`CATEGORY_EN`（类别 role→英文，42 项，role 见 analysis-data.js 各 category）、`RENDER_DIM_EN`（渲染维度 id→英文，15 项）
  - 辅助函数 `getModuleEn / getCatEnRole / getDimEn`；英文模式下前缀用英文名，中文/`both` 模式保持原样（both 模式前缀中文与词条中文 label 配对）
  - 类别英文名从 `findModule(mod.id).categories[catIdx].role` 查表（role 唯一）；渲染维度英文名按 `dim.id` 查表
- **效果示例(英文)**: `01-Input Control-Geometry Lock:Building morphology / form` / `-Context Lock:Lake shoreline` / `01-Reference Source:Reference building precedent`
- **测试**: app.js 语法 OK；prompt-engine 19/19 通过
- **部署**: CloudStudio ✅（a0d762f）；GitHub push 本次成功（仓库迁移但旧 remote gsd-yuexia 仍可用，已推到 feature/prompt-sentence-engine）→ GitHub Pages 同步更新

## 用 RP.xlsx 替换效果图数据 + 统一向导 + 部署 (2026-07-07 续，跨会话)
- **需求**: 用 RP.xlsx (Sheet1, 13 维度 维度0-维度12) 内容替换现有效果图数据，结构参考 analysis-data.js 三级结构 (module→categories→items)
- **数据生成**: `gen_data.py` 解析 RP.xlsx Sheet1 → 生成 `data.js` (13 模块, 213 词条, 三级结构)；Excel 维度号 0-12 → 模块 id "1"-"13"；维度0 拆 轻度/中度/高度 三类别 (role constraint_light/medium/high)；视角(维度8) 词条加 `extra:"焦距: X"` / `effect:"Y"`
- **app.js 重构 (Task #34)**: 删除效果图旧全屏面板死代码 (renderRenderingDimensions 等)，统一效果图与分析图向导路径 — 共享 `moduleId::catIdx::itemIdx` 选择键与 `toggleAnalysisSelection`；`buildSteps/renderWizardContent/onWizardTagClick/stepHasSelection/countStepSelections/getStepDotClass/updateSelectedTags/buildSelectionsList/buildKeywordTree/updatePrompt/reRenderTags/updateNavDot*/updateDimensionCounts` 全部统一到模块模型；新增 `RENDER_MODULE_EN`(13项) 与 `getRenderModuleEn`；扩展 `CATEGORY_EN` 新 role；AI 图片解析 `matchKeywordsToVocabulary/renderAnalysisResults/applyMatchedTags/buildAnalysisPrompt` 统一为模块模型 (数据 id 现为字符串 "1"-"13"，匹配 AI JSON 维度键)
- **PromptEngine 修复**: `buildRenderSentenceEN` 默认主语 `'a hospital building'` → `'hospital building'`，修复 "An a hospital building." 双冠词 bug
- **测试重写 (Task #33)**: `test/data.test.js` (13 模块/字段/role 合法集/模块1 三类别/模块9 焦距/总词条≥200) + `test/prompt-engine.test.js` (新 role 分类)；`node --test` → 22/22 通过
- **部署 (Task #35)**:
  - ⚠️ **关键纠正**: GitHub Pages 实际发布分支是 **`gh-pages`，不是 `master`**！推 master 不会更新线上站点（origin/gh-pages 有 "触发 Pages 重建" 提交佐证）。本次先推 master(a6fc646) 后站点仍显示旧 15 维度 → 确认发布分支为 gh-pages → 执行 `git checkout -B gh-pages origin/gh-pages && git merge --ff-only master && git push origin gh-pages`(04c64a6..a6fc646) → 站点才真正更新为 RP.xlsx 13 维度内容（用 `?cb=3` 缓存破解 WebFetch 15分钟缓存后验证 data.js 已为 13 模块、首模块"约束类型"）
  - CloudStudio: `workbuddy_cloudstudio_deploy` 部署 `dist/` → https://8f8b24d09f264b94b6d00ed50d365b07.app.codebuddy.work
  - 根仓库: `feature/prompt-sentence-engine` → commit 82a0ef1 + 内存提交 9effe10 + push
- **注意**: `dist/` 是嵌套仓库 (master=GitHub Pages)；根仓库 feature 分支；remote 已迁移到 GSD-YueXia (旧 gsd-yuexia 仍可用)；jsdom 不可用，用语法检查 + engine smoke test + grep dangling ref 验证
